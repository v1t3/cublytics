<div class="header header-main">
    <div class="logo"></div>
    <div class="top-menu">
        <div class="top-left links">
            <a href="{{ path('main') }}" class="top-menu-link">Главная</a>
            <a href="{{ path('about_page') }}" class="top-menu-link">Где я?</a>
        </div>
        <div class="top-right links">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('spa') }}" class="top-menu-link">Кабинет</a>
                <a href="{{ path('app_logout') }}" class="top-menu-link">Выйти</a>
            {% else %}
                <a href="{{ path('app_login_coub') }}" class="top-menu-link">Войти</a>
            {% endif %}
        </div>
    </div>
</div>
